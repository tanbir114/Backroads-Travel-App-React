# Backroads App -> a simple react one screen/page website for basic understanding of react

- How to run it?
  - navigate to the folder
  - run 'npm install'
  - once the dependencies are installed run 'npm start'
